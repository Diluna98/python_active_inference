# PyAIF

PyAIF is a Python package for building and running Active Inference agents and simulations. It provides a clean interface to the core AIF class and example simulations for testing and experimentation.

## Installation

Clone the repository and install in editable mode:

```bash
git clone https://github.com/diluna98/active_inference.git
cd active_inference
pip install -e .
